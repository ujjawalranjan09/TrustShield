# Phase F: Quality & Testing — Implementation Plan

> **Goal:** Fill critical testing gaps, fix broken features, consolidate schemas, and establish E2E testing — making the codebase production-grade.

**Tech Stack:** pytest, pytest-asyncio, httpx (TestClient), Vitest (frontend), Playwright (E2E)

---

## F1 — API Router Unit Tests (12 untested routers)

**Cover every untested router with unit tests using mocked dependencies.**

### F1.1 — High-Priority Routers (core business logic)

| Router | Test File | Key Assertions |
|--------|-----------|----------------|
| `analytics.py` | `test_analytics.py` | Dashboard stats return correct shape, date filtering works |
| `batch.py` | `test_batch.py` | Batch analyze processes multiple sessions, returns aggregated results |
| `scan.py` | `test_scan.py` | Scan endpoint classifies messages, returns risk score + entities |
| `intel.py` | `test_intel.py` | Bank registration returns API key, entity sharing works, cross-bank lookup aggregates |
| `compliance.py` | `test_compliance.py` | RBI report generates PDF, DPDP register returns assets |
| `dpdp.py` | `test_dpdp.py` | Data register CRUD, consent recording, export works |

**Pattern per test file:**
- Mock DB session with `AsyncMock`
- Mock auth dependency (use `app.dependency_overrides`)
- Test happy path + error cases
- Test tenant isolation (returns only current tenant's data)

### F1.2 — Medium-Priority Routers

| Router | Test File | Key Assertions |
|--------|-----------|----------------|
| `consumer.py` | `test_consumer.py` | Public endpoints accessible without auth, rate-limited |
| `hotspots.py` | `test_hotspots.py` | Geographic aggregation returns correct buckets |
| `recovery.py` | `test_recovery.py` | Case creation, status updates, audit trail |
| `report.py` | `test_report.py` | PDF generation returns valid bytes, includes required sections |
| `ws_dashboard.py` | `test_ws_dashboard.py` | WebSocket connects, receives events, handles disconnect |
| `tenant.py` | `test_tenant_api.py` | Provision/offboard via API endpoints (service tests exist, API-level don't) |

**Total: 18 new test files, ~90-120 test cases**

---

## F2 — Schema Consolidation

**Current state:** 3 schema modules (`analyze.py`, `risk.py`, `entity.py`) for 31 routers — most routers have inline Pydantic models.

### F2.1 — Create Missing Schema Modules

Create shared response/request schemas:

| File | Contents |
|------|----------|
| `schemas/common.py` | `PaginatedResponse[T]`, `ErrorResponse`, `SuccessResponse`, `HealthResponse` |
| `schemas/analytics.py` | `DashboardStats`, `TimeSeriesPoint`, `HotspotData` |
| `schemas/batch.py` | `BatchRequest`, `BatchResponse`, `BatchResult` |
| `schemas/compliance.py` | `RBIReportResponse`, `DPDPRegisterEntry`, `DataAssetResponse` |
| `schemas/recovery.py` | `RecoveryCaseCreate`, `RecoveryCaseResponse`, `CaseStatusUpdate` |
| `schemas/intel.py` | `BankRegisterRequest`, `BankRegisterResponse`, `EntityShareRequest`, `CrossBankLookupResponse` |
| `schemas/scim.py` | `SCIMUser`, `SCIMGroup`, `SCIMPatchOp` |

### F2.2 — Refactor Existing Routers

For each router with inline schemas:
1. Extract inline Pydantic models to the new schema module
2. Update imports in the router
3. Ensure response_model is set on every endpoint (FastAPI auto-docs)

**Verification:** `ruff check`, all existing tests still pass, OpenAPI spec improves (proper schema refs)

---

## F3 — Fix Broken Image Analysis

**Current state:** `pyzbar` and `Pillow` removed from requirements.txt due to Windows DLL issues. QR code detection is non-functional.

### F3.1 — Restore with Graceful Degradation

1. Add `Pillow>=10.0.0` to requirements.txt (works cross-platform)
2. Add `pyzbar>=0.1.9` to requirements.txt with conditional import
3. In `image_analysis.py`: wrap pyzbar import in try/except, degrade to OCR-only when unavailable
4. Create `backend/tests/unit/test_image_analysis_fixed.py`:
   - Test with pyzbar available (mock the import)
   - Test graceful degradation when pyzbar unavailable
   - Test QR decode extracts UPI/phone/URL entities

---

## F4 — Frontend Testing Setup

**Current state:** 0 frontend test files, no test framework configured.

### F4.1 — Setup Vitest

1. Add `vitest` + `@testing-library/react` + `@testing-library/jest-dom` to devDependencies
2. Create `frontend/vitest.config.ts`
3. Create `frontend/tests/setup.ts` with jest-dom matchers

### F4.2 — Component Tests

Test critical UI components:

| Component | Test File | Assertions |
|-----------|-----------|------------|
| VerdictCard | `VerdictCard.test.tsx` | Renders risk badge, entity list, recommended action |
| RiskBadge | `RiskBadge.test.tsx` | Color coding for low/medium/high/critical |
| EntityList | `EntityList.test.tsx` | Masks PII, shows entity type icons |
| ScanForm | `ScanForm.test.tsx` | Submit handler called, validation works |
| ReportForm | `ReportForm.test.tsx` | Form submission, 429 handling |
| ReputationBadge | `ReputationBadge.test.tsx` | Green/yellow/red for clean/watch/suspicious |

### F4.3 — Page-Level Tests

| Page | Test File | Assertions |
|------|-----------|------------|
| `/analyze` | `analyze.test.tsx` | Tab switching, API calls per tab, verdict rendering |
| `/report` | `report.test.tsx` | Form submission, success message, rate limit |
| `/check` | `check.test.tsx` | Entity lookup, reputation badge display |

**Total: ~15-20 frontend test files**

---

## F5 — SDK Tests

**Current state:** 0 tests across all 3 SDKs.

### F5.1 — Web SDK Tests

1. Add `vitest` to `sdk/web/`
2. Create `sdk/web/tests/trustshield.test.ts`:
   - Test analyzeChat returns typed Verdict
   - Test retry logic (mock 503 → retried with backoff)
   - Test PII redaction in debug mode
   - Test offline queue (enqueue → flush on reconnect)
   - Test error handling (network error, timeout)

### F5.2 — iOS SDK Tests

1. Create `sdk/ios/Tests/TrustShieldSDKTests/TrustShieldTests.swift`:
   - Test analyzeChat returns Verdict struct
   - Test retry logic
   - Test offline queue
   - Test PII redaction

### F5.3 — Android SDK Tests

1. Create `sdk/android/TrustShieldSDK/src/test/java/com/trustshield/TrustShieldManagerTest.kt`:
   - Test analyzeMessage returns Verdict data class
   - Test retry logic
   - Test offline queue (SharedPreferences)
   - Test PII redaction

---

## F6 — E2E Test Framework

**Current state:** No E2E testing framework.

### F6.1 — Playwright Setup

1. Add `@playwright/test` to frontend devDependencies
2. Create `frontend/playwright.config.ts`
3. Create `frontend/e2e/` directory

### F6.2 — Critical User Flows

| Flow | Test File | Steps |
|------|-----------|-------|
| Login → Analyze → View Result | `auth-analyze.spec.ts` | Register, login, submit message, verify verdict displayed |
| Report Scam → Check Reputation | `report-check.spec.ts` | Submit report, lookup entity, verify reputation updated |
| SSO Login (mocked) | `sso-login.spec.ts` | Click SSO, mock IdP callback, verify JWT issued |
| Admin: Manage Roles | `admin-roles.spec.ts` | Login as admin, create role, assign to user, verify permission |

---

## F7 — Ad-Hoc Test Cleanup

**Current state:** 9 test scripts at project root (`test_db_queries.py`, `test_endpoints.py`, etc.)

1. Audit each script — move useful tests into `backend/tests/unit/` or `backend/tests/integration/`
2. Delete scripts that duplicate existing tests
3. Delete scripts that are obsolete

---

## Execution Order

```
F1 (API tests) + F3 (fix image) — parallel, independent
  → F2 (schema consolidation) — after F1 to avoid merge conflicts
  → F4 (frontend tests) + F5 (SDK tests) — parallel, independent
  → F6 (E2E framework) — after F4
  → F7 (cleanup) — last
```

## Verification

After each task:
1. `cd backend && python -m pytest tests/unit/ -v` — all tests pass
2. `python -m ruff check .` — clean
3. `cd frontend && npm run test` — frontend tests pass (after F4)
4. `cd frontend && npx playwright test` — E2E tests pass (after F6)

## Exit Gate

- [ ] 12 previously-untested routers now have unit tests
- [ ] All 31 routers have at least 1 test
- [ ] Image analysis QR decode works (with graceful fallback)
- [ ] Frontend has Vitest + component tests for critical UI
- [ ] All 3 SDKs have test suites
- [ ] E2E framework configured with critical flow tests
- [ ] Ad-hoc test scripts cleaned up
- [ ] `ruff check .` clean, `npm run lint` clean
- [ ] Total test count ≥ 200 (from current ~170)
